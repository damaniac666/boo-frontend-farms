import { MasterChefConfig } from '../hooks/masterchef/useMasterChefV2';

     const masterChefConfig: MasterChefConfig = {
       contractName: 'MasterChefV2', // Update with deployed contract name
       nexus: 'mainnet',
     };

     export default masterChefConfig;